//* 5. Check if a number is within a range between 10 and 20 (inclusive).

let num = 10;
let result = (num >= 10 && num <= 20) ? "Number is within a range between 10 and 20" : "Number is not within range!";

console.log(result);
